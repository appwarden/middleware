# Example environment variables for local development and e2e tests
# Copy this file to `.env` and fill in the real values.

# API token used by e2e tests to control quarantine mode via the staging API
APPWARDEN_API_TOKEN=
