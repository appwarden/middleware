name: Trigger Skill Review

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "src/**"

permissions:
  contents: write

jobs:
  notify:
    name: Trigger Skill Review
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Collect changed files
        id: changes
        run: |
          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"
          if [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            FILES=$(git show --name-only --pretty="" "$AFTER" | jq -R -s -c 'split("\n") | map(select(length > 0))')
          else
            FILES=$(git diff --name-only "$BEFORE" "$AFTER" | jq -R -s -c 'split("\n") | map(select(length > 0))')
          fi
          echo "files=$FILES" >> "$GITHUB_OUTPUT"

      - name: Dispatch to intent repo
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          event-type: skill-check
          client-payload: |
            {
              "package": "@appwarden/middleware",
              "sha": "${{ github.sha }}",
              "changed_files": ${{ steps.changes.outputs.files }}
            }
